# Markdown Code Block Fixture

```python
def trusted_answer(source_count: int) -> bool:
    return source_count > 0
```

The phrase code block preserved confirms fenced content stays available for retrieval.
